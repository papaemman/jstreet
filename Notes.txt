=============================
Project Notes
=============================

// Get dataset //
$ kaggle competitions download -c jane-street-market-prediction

// Directory structure //

My directory structure isn't completely correct.

How to read datasets?
train = pd.read_csv("../input/jane-street-market-prediction/train.csv")


// Activate virtual environment //
$ source /opt/conda/bin/activate
$ conda activate base


// Save current state of modules versions //
$ pip freeze > requirements.txt


// Install new modules using pip, but don't update exchisting modules //
$ pip install module_name
$ pip list


// Start jupyter-notebook //

$ docker exec -ti jstreet-container bash
$ jupyter-notebook --no-browser --ip=0.0.0.0 --port=8888 --allow-root

Create ssh tunneling from local machine
(local) $ ssh -N -f -L localhost:8888:172.17.0.2:8888 panagiotis.medoid@tiger.ngsqintl.com

$ docker inspect -f '{{range .NetworkSettings.Networks}}{{.IPAddress}}{{end}}' jstreet-container

Open Jupyter in localhost:8888
